<UserControl x:Class="PokeApp.Views.ShopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PokeApp.Views"
             xmlns:viewmodel="clr-namespace:PokeApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewmodel:ShopVM/>
    </UserControl.DataContext>
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/PokeApp;component/Views/Media/Images/ShopBackground.png"
                    Stretch="Fill"/>
        </Grid.Background>

        <Canvas Name="AnimationCanvas">
            <Image Name="CharacterSprite"
           Width="64" Height="64"
           Canvas.Left="260" Canvas.Top="410"/>
        </Canvas>

        <Grid Name="ShopPanel" Visibility="Visible">
            <Canvas HorizontalAlignment="Left" VerticalAlignment="Top" Width="250" Height="300" Margin="550,0,0,0">

                <Image Source="pack://application:,,,/PokeApp;component/Views/Media/Images/ShopUI.png"
                   Stretch="Fill"
                   Width="225" Height="250"
                   Canvas.Left="0" Canvas.Top="0"/>

                <ItemsControl ItemsSource="{Binding Inventory}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Width="180" Height="40"
                                Content="{Binding Name, StringFormat={}{0} - {Binding Price}₽}"
                                Command="{Binding DataContext.BuyItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Left" Value="30" />
                            <Setter Property="Canvas.Top" Value="50"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                </ItemsControl>
            </Canvas>
        </Grid>

    </Grid>
</UserControl>
